<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OMNIFIT</title>
    <style>
        /* General Styles */
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            color: #333;
            background-color: #f7f9fc;
        }

        /* Hero Section */
        .hero {
            position: relative;
            background: url('hero-image.jpg') no-repeat center center/cover;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6); /* Gradient overlay */
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .hero-content h1 {
            font-size: 3.5rem;
            margin-bottom: 20px;
        }

        .hero-content p {
            font-size: 1.2rem;
            margin-bottom: 30px;
        }

        .cta-button {
            background-color: #ff6f61;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .cta-button:hover {
            background-color: #ff8c42;
        }

        /* Features Section */
        .features {
            padding: 50px 20px;
            text-align: center;
            background-color: #ffffff;
        }

        .features h2 {
            font-size: 2.5rem;
            margin-bottom: 30px;
        }

        .feature-list {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
        }

        .feature {
            max-width: 300px;
            margin: 20px;
            text-align: center;
        }

        .feature img {
            max-width: 80px;
            margin-bottom: 20px;
        }

        /* Footer */
        .footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px;
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <header class="hero">
        <div class="hero-overlay"></div>
        <div class="hero-content">
            <h1>Transform Your Fitness Journey with OMNIFIT</h1>
            <p>Personalized fitness plans, expert coaching, and a supportive community await you.</p>
            <a href="#features" class="cta-button">Get Started</a>
        </div>
    </header>

    <!-- Features Section -->
    <section class="features" id="features">
        <h2>Why Choose OMNIFIT?</h2>
        <div class="feature-list">
            <div class="feature">
                <img src="state-of-the-art.png" alt="State-of-the-Art Equipment">
                <h3>State-of-the-Art Equipment</h3>
                <p>Train with the best tools for optimal results.</p>
            </div>
            <div class="feature">
                <img src="personalized-programs.png" alt="Personalized Programs">
                <h3>Personalized Programs</h3>
                <p>Custom workout plans tailored to your goals.</p>
            </div>
            <div class="feature">
                <img src="community-support.png" alt="Community Support">
                <h3>Community Support</h3>
                <p>Be part of a motivated and supportive community.</p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <p>&copy; 2024 OMNIFIT. All rights reserved.</p>
    </footer>
</body>
</html>
